import 'package:flutter/material.dart';

class AppColors {
  // Primary & Accents
  static const Color primary = Color(0xFFC6FF00); // Bright lime-green
  
  // Neutral Colors
  static const Color dark = Color(0xFF151A1A); // Dark charcoal
  static const Color background = Color(0xFFF7F7F5);
  static const Color surface = Color(0xFFFFFFFF);
  
  // Text Colors
  static const Color textPrimary = Color(0xFF151A1A);
  static const Color textSecondary = Color(0xFF7A7A7A);
  
  // Semantic Colors
  static const Color error = Color(0xFFFF4D4D);
}
